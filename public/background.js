// import OptionBar from "../src/options/OptionBar";

// chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
//     if (request.message === "textSelected") {
//         OptionBar(request.getSelectedText);
//     } 
// });
