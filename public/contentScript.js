// import OptionBar from "../src/options/OptionBar.jsx"

// const getSelectedText = () => window.getSelection().toString();

// document.addEventListener("selectionchange", () => {
//     return getSelectedText.length > 0 ? <OptionBar text={getSelectedText}></OptionBar> : null;
//         // call background script to show option bar
//         // chrome.runtime.sendMessage({ message: "textSelected", getSelectedText });
// });
